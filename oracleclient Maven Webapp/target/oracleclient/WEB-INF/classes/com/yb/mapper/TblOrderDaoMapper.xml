<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd" >
<mapper namespace="com.yb.dao.TblOrderDao">

		<select id="queryAll" parameterType="Integer" resultType="TblOrder">
			select ORDER_ID as orgId,VERSION,AMOUNT,ORDERNO,TRADEID,CARDUSER_ID as carduserId,CREATEDATE,
			
			ORDER_STATUS as orderStatus,UPDATEDATE,OPENID from TBL_ORDER where ORDER_ID >#{id}
			
		</select>
		
		<select id="queryMaxId" resultType="Integer">
  		select max(ORDER_ID) from TBL_ORDER
  	</select>
</mapper>
