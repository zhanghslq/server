<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd" >
<mapper namespace="com.yb.dao.TblShoppingcartDao">
		<select id="queryAll" parameterType="Integer" resultType="TblShoppingcart"> 
			select SHOPPINGCART_ID as shoppingcartId,VERSION,CREATETIME,RULESET_ID as rulesetId,
			
			CARDUSER_ID as carduserId,INTEGRATION,CUSTOMER_LEVEL as customerLevel,GOODSNUM,VIRTUAL_GOODS_TYPE as virtualGoodsType from TBL_SHOPPINGCART
			
			where SHOPPINGCART_ID>#{id}
		</select>	
		
		
		<select id="queryMaxId" resultType="Integer">
  		select max(SHOPPINGCART_ID) from TBL_SHOPPINGCART
  	</select>
</mapper>
